diff --git a/.github/workflows/pr-checks.yml b/.github/workflows/pr-checks.yml
index 438470d..a5698d2 100644
--- a/.github/workflows/pr-checks.yml
+++ b/.github/workflows/pr-checks.yml
@@ -6,25 +6,22 @@ on:
       - main
 
 jobs:
-  code-quality:
+  ruff:
     runs-on: ubuntu-latest
-    
     steps:
       - uses: actions/checkout@v4
-      
+        with:
+          fetch-depth: 0
+          
       - name: Set up Python
-        uses: actions/setup-python@v5
+        uses: actions/setup-python@v4
         with:
           python-version: '3.x'
           
-      - name: Install dependencies
-        run: |
-          python -m pip install --upgrade pip
-          pip install ruff pytest
-          pip install -r requirements.txt
-          
-      - name: Run Ruff
-        run: ruff check .
+      - name: Install Ruff
+        run: pip install ruff
         
-      - name: Run Tests
-        run: pytest 
\ No newline at end of file
+      - name: Run Ruff on changed lines
+        run: |
+          git fetch origin main:main
+          git diff main...HEAD | ruff check --diff -
\ No newline at end of file
diff --git a/src/calculadora.py b/src/calculadora.py
index 1a89014..851d238 100644
--- a/src/calculadora.py
+++ b/src/calculadora.py
@@ -17,4 +17,10 @@ class Calculadora:
         """Divide dois números."""
         if b == 0:
             raise ValueError("Não é possível dividir por zero")
-        return a / b
\ No newline at end of file
+        return a / b
+    
+    def potencia(self, a: float, b: float) -> float:
+        """Calcula a potência de um núgit diff main...HEAD | rgit diff main...HEAgit diff main...HEAD | ruff check --diff -D | ruff check --diff -uff check --diff -mero."""
+        return a ** b + c
+    
+
